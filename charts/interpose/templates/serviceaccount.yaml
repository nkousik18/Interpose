{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "interpose.serviceAccountName" . }}
  labels:
    {{- include "interpose.labels" . | nindent 4 }}
{{- end }}
